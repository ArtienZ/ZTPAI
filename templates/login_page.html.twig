<!DOCTYPE html>
<html lang="en">
<head>
    {% block stylesheets %}
        <link rel="stylesheet" type="text/css" href="{{ asset('css/style.css') }}">
        <link rel="stylesheet" type="text/css" href="{{ asset('css/main-page.css') }}">
        <script src="{{ asset('https://kit.fontawesome.com/34d5627880.js') }}" crossorigin="anonymous"></script>
    {% endblock %}
    <meta charset="UTF-8">
    <title>LOGIN PAGE</title>
</head>
<body>
{% if error %}
<div>{{ error.messageKey|trans(error.messageData,'security') }}</div>
{% endif %}
<div class="base-container">

    <header>
        <img class="logo" src="{{ asset('img/logo.svg') }}" alt="logo">
        <nav>

            <ul class = "menu">

                <li>
                    <a href="login_page" class="nav-button">ZALOGUJ SIĘ</a>
                </li>
            </ul>
        </nav>
    </header>
    <main>
        <div class="photo">
            <img src="{{ asset('img/home-page-photo.svg') }}" alt="photo">
        </div>
        <div class="about">
            <div class="login_container">
                <form action="{{ path('login') }}" class="login_box" method="POST">
                    <div class="messages">
                        <?php
                    if(isset($messages)){
                        foreach ($messages as $message){
                            echo $message;
                        }
                    }
                    ?>
                    </div>
                    <input class="login_inputs" name="email" type="text" value="{{ last_username }}" placeholder="LOGIN">
                    <input class="login_inputs" name="password" type="password" placeholder="HASLO">
                    <input type = "hidden" name="_target_path" value="/my_profile"/>
                    <button type="submit">ZALOGUJ</button>
                </form>
            </div>
        </div>
    </main>
</div>
</body>
</html>

